import {Colors} from '../../Themes/'

export default {
  drawer: {
    backgroundColor: Colors.background,
    shadowColor: '#000000',
    shadowOpacity: 0.8,
    shadowRadius: 0
  },
  main: {
    backgroundColor: Colors.clear
  }
}
